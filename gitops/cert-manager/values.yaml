crds:
  enabled: true

# pihole answers letusseng.com locally, so the DNS-01 propagation check has to ask
# public resolvers or it never sees the _acme-challenge TXT record on Cloudflare
extraArgs:
  - --dns01-recursive-nameservers-only
  - --dns01-recursive-nameservers=1.1.1.1:53,9.9.9.9:53
